{%  extends '::layout/layout.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <h2>{{ page.titre }}</h2>
            <div class="span12">
                    {{ page.contenu | raw }}
            </div>
        </div>
    </div>
{% endblock %}